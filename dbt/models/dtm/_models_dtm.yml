version : 2

models:
  - name: dim_client
    description: 'Dimension des clients'
    columns:
      - name: id_client
        description: 'id du client'
        tests: [unique, not_null]
  - name: dim_produit
    description: la dimension des produits
    columns:
      - name: id_produit
        tests: [unique, not_null]
  - name: dim_ville
    description: la dimension des villes
    columns:
      - name: id_ville
        tests: [unique, not_null]
  # à compléter
